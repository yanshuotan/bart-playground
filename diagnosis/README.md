# Diagnosis results

This directory contains only the current, paper-facing results.

- `analysis/`: the retained Abalone notebook, reusable analysis scripts, and
  generated analysis outputs. Other dataset-specific notebooks are archived.
- `store/`: current fixed-100 experiment data consumed by the analysis.