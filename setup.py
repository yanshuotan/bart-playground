"""
Minimal setup.py for backward compatibility.

All configuration is now in pyproject.toml.
This file is kept for compatibility with older versions of pip and setuptools.
"""
from setuptools import setup

setup()
